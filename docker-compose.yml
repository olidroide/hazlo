services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: hazlo
      POSTGRES_PASSWORD: hazlo
      POSTGRES_DB: hazlo
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
